public class mat {
    public static void main(String[] args) {
      int x = Math.min(5, 2);
      int y = Math.max(4, 10);
      double z = Math.ceil(23.2);
      double d = Math.floor(23.2);
      double random = Math.random();
      System.err.printf("x: %d\ny: %d\nz: %.2f\nd: %.2f\nrandom: %f\n",x,y,z,d,random);
    }
}
